#include "a.h"

void printPart(vi p);

bool next(vi &p, vi &m);

//int solve(int n, int k, int m) {
//    vi p(n, 0); // p is the actual partition
//    vi max(n, 0); // m[i] is max_{i <= j <= n-1} p[j]
//
//    int tot = 1;
//    while (next(p, max))
//        tot++;
//    return tot;
//}
